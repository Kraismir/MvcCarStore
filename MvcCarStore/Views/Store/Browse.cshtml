@model MvcCarStore.Models.Brand

@{
    ViewBag.Title = "Browse";
}

<h2>Browsing Brand: @Model.CarBrand </h2>
<ul>
    @foreach (var car in Model.Cars)
    {
        <li>
            @Html.ActionLink(car.Model, "Details", new {id = car.CarID});
            
        </li>
    }
</ul>
